package model;

import java.security.Timestamp;
import java.util.Date;

public class Country extends Timestamped {
    private int id;                  // Country_ID INT(10) (PK)
    private String name;             // Country VARCHAR(50)

}
